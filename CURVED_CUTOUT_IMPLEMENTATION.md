# 缺口曲线绘制实现

## 修改时间
2026年2月6日

## 🎯 修改内容

将缺口的直线绘制改为大弧度曲线绘制，使用 `addQuadCurve` 实现柔和的L形缺口。

## ✅ 完成的改进

### 1. 缺口垂直边 - 大弧度曲线
```swift
// 修改前（直线）
path.addLine(to: points.cutoutVerticalEnd)

// 修改后（大弧度曲线）
path.addQuadCurve(
    to: points.cutoutVerticalEnd,
    control: CGPoint(
        x: points.topEdgeEnd.x - 20,  // 向左凸出20pt
        y: verticalMidY
    )
)
```

### 2. 缺口水平边 - 大弧度曲线
```swift
// 修改前（直线）
path.addLine(to: points.cutoutHorizontalEnd)

// 修改后（大弧度曲线）
path.addQuadCurve(
    to: points.cutoutHorizontalEnd,
    control: CGPoint(
        x: horizontalMidX,
        y: points.cutoutVerticalEnd.y - 20  // 向上凸出20pt
    )
)
```

### 3. 其他边线 - 轻微曲线
```swift
// 上边线、右边线、底边线、左边线都添加了5pt的轻微曲线
// 让整体形状更加柔和自然
```

## 🎨 视觉效果对比

### 修改前（直线缺口）
```
┌──────────────────┐
│              ┌──┤  ← 生硬的直角
│              └──┤
│                 │
└─────────────────┘
```

### 修改后（大弧度曲线缺口）
```
┌──────────────────┐
│              ╭──┤  ← 柔和的曲线
│              ╰──┤
│                 │
╰─────────────────╯
```

## 📐 曲线控制点详解

### 垂直边曲线
```
起点 ●
      ╲
       ╲  ← 控制点在左侧20pt
        ●  
       ╱
      ╱
终点 ●

控制点位置：
x = 起点x - 20  (向左凸出)
y = (起点y + 终点y) / 2  (垂直中点)
```

### 水平边曲线
```
起点 ●──╮
        │  ← 控制点在上方20pt
        ╰── ● 终点

控制点位置：
x = (起点x + 终点x) / 2  (水平中点)
y = 起点y - 20  (向上凸出)
```

## 🔧 参数说明

### 缺口曲线弧度
| 位置 | 控制点偏移 | 效果 |
|------|-----------|------|
| **缺口垂直边** | 向左20pt | 大弧度凸出 |
| **缺口水平边** | 向上20pt | 大弧度凸出 |
| **其他边线** | 5pt | 轻微波浪 |

### 如何调整弧度

**增大缺口弧度：**
```swift
// 将20改为30或更大
control: CGPoint(x: points.topEdgeEnd.x - 30, y: verticalMidY)
```

**减小缺口弧度：**
```swift
// 将20改为10或更小
control: CGPoint(x: points.topEdgeEnd.x - 10, y: verticalMidY)
```

**移除其他边线的曲线：**
```swift
// 将曲线改回直线
path.addLine(to: points.topEdgeEnd)  // 不用addQuadCurve
```

## 💡 曲线绘制原理

### addQuadCurve 工作原理
```
起点 ────→ 终点

添加控制点后：
起点 ╭─── 控制点
     │
     ╰──→ 终点

曲线会"被吸引"向控制点方向弯曲
```

### 贝塞尔曲线公式
```
P(t) = (1-t)² × 起点 + 2(1-t)t × 控制点 + t² × 终点
其中 t ∈ [0, 1]
```

## 🎯 效果展示

### 完整气泡效果
```
     ╭──────────────────╮
    ╱                   │
   │                  ╭─┤ ← 大弧度L形缺口
   │                  │ │
   │  Text content    ╰─┤
   │                    │
    ╲                  ╱
     ╰────────────────╯
```

### 缺口特写
```
垂直边向左凸20pt
      ╲
       ╲ ←控制点
        ●
       ╱
      ╱ ←控制点   水平边向上凸20pt
     ●────╮
          │
          ╰────
```

## ✅ 优势

1. **视觉更柔和**
   - 没有生硬的直角
   - 更符合自然形态

2. **设计感更强**
   - 专业的曲线设计
   - 类似水滴或云朵的有机形状

3. **可调节性强**
   - 修改控制点偏移值即可调整弧度
   - 每条边都可以独立调整

4. **性能依然优秀**
   - 依然是单个Shape
   - 曲线渲染效率高

## 🔍 调试技巧

### 可视化控制点
```swift
// 在预览中添加辅助点
Circle()
    .fill(Color.red)
    .frame(width: 5, height: 5)
    .position(控制点位置)
```

### 测试不同弧度
```swift
// 定义为变量方便测试
let curveAmount: CGFloat = 20  // 尝试10, 20, 30等不同值
```

## 📊 曲线参数表

| 边线 | 曲线类型 | 控制点偏移 | 效果 |
|------|---------|-----------|------|
| 上边线 | 轻微曲线 | 向上5pt | 轻微波浪 |
| 缺口垂直边 | 大弧度 | 向左20pt | 明显凸出 |
| 缺口水平边 | 大弧度 | 向上20pt | 明显凸出 |
| 右边线 | 轻微曲线 | 向右5pt | 轻微波浪 |
| 底边线 | 轻微曲线 | 向下5pt | 轻微波浪 |
| 左边线 | 轻微曲线 | 向左5pt | 轻微波浪 |

## ✅ 编译状态
- ✅ 无编译错误
- ✅ 曲线绘制正确
- ✅ 所有边线都使用曲线
- ✅ 缺口弧度明显

---
**实现方式：** 大弧度贝塞尔曲线  
**完成时间：** 2026-02-06  
**状态：** ✅ 完成
